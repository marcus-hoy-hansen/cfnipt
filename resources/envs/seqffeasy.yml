name: seqff-env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # R
  - r-base>=4.3
  # CRAN pkgs
  - r-argparse
  - r-dplyr
  - r-stringr
  - r-readr        # common helper (optional)
  # Bioconductor
  - bioconductor-rsamtools
  # (optional) useful runtime deps
  - r-optparse
